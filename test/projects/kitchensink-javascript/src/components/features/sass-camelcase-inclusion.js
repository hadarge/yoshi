import React from 'react';
import styles from './assets/style.sass';

export default () => (
  <div>
    <p id="sass-camelcase-inclusion" className={styles.sassModulesInclusion}>
      CSS Modules are working!
    </p>
  </div>
);
