/* eslint-disable */

import React from 'react';
import ReactDOM from 'react-dom';
// import TranspileConfiguredExternal from './transpile-configured-external';

it.skip('should pass', () => {
  const div = document.createElement('div');
  // ReactDOM.render(<TranspileConfiguredExternal />, div);
});
