import React from 'react';
import ReactDOM from 'react-dom';
// import TranspileDefaultExternal from './transpile-default-external';

it.skip('should pass', () => {
  const div = document.createElement('div');
  // ReactDOM.render(<TranspileDefaultExternal />, div);
});
