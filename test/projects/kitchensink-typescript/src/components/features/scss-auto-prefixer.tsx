import React from 'react';
import styles from './assets/auto-prefixer.scss';

export default () => (
  <div id="scss-auto-prefixer" className={styles['scss-auto-prefixer']}>
    Auto Prefixer is working!
  </div>
);
